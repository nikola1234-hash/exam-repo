<Window x:Class="EasyTestMaker.MainWindow"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:hc="https://handyorg.github.io/handycontrol"
                Title="Exam Management" WindowState="Maximized" WindowStartupLocation="CenterScreen"
                xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors" 
               xmlns:viewmodels="clr-namespace:EasyTestMaker.ViewModels" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" >
    
    <Window.Resources>
        <ResourceDictionary>
            <BooleanToVisibilityConverter x:Key="BoolToVis" />
        </ResourceDictionary>

    </Window.Resources>
    
    <Grid>
        <behaviors:Interaction.Triggers>
            <behaviors:EventTrigger EventName="Loaded">
                <behaviors:InvokeCommandAction Command="{Binding Loaded}" />
            </behaviors:EventTrigger>
        </behaviors:Interaction.Triggers>
        <ContentControl Content="{Binding CurrentViewModel}" />
    </Grid>
</Window>
