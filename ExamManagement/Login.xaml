<Window x:Class="EasyTestMaker.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EasyTestMaker"
        mc:Ignorable="d"
        Title="Login" Height="200" Width="400"
        DataContext="{Binding RelativeSource={RelativeSource Self}}" WindowStartupLocation="CenterScreen"  ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto">
                
            </RowDefinition>
            <RowDefinition Height="*">

            </RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Vertical" VerticalAlignment="Center" Margin="0" Width="200" HorizontalAlignment="Center">
            <TextBlock HorizontalAlignment="Center" FontSize="13" Text="Username"></TextBlock>
            <TextBox Margin="0 5 0 5" FontSize="15" x:Name="username"></TextBox>
            <TextBlock HorizontalAlignment="Center" FontSize="13" Text="Password"></TextBlock>
            <TextBox  Margin="0 5 0 15" FontSize="15" x:Name="password"></TextBox>
            <Button Click="Button_Click" FontSize="13">Login</Button>
        </StackPanel>
    </Grid>
</Window>
