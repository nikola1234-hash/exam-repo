<UserControl x:Class="EasyTestMaker.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EasyTestMaker.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto">

            </RowDefinition>
            <RowDefinition Height="*">

            </RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Vertical" VerticalAlignment="Center" Margin="0" Width="200" HorizontalAlignment="Center">
            <TextBlock HorizontalAlignment="Center" FontSize="13" Text="Username"></TextBlock>
            <TextBox Margin="0 5 0 5" FontSize="15" Text="{Binding Username}"></TextBox>
            <TextBlock HorizontalAlignment="Center" FontSize="13" Text="{Binding Password}"></TextBlock>
            <TextBox  Margin="0 5 0 15" FontSize="15" x:Name="password"></TextBox>
            <Button Command="{Binding Login}" FontSize="13">Login</Button>
        </StackPanel>
    </Grid>
</UserControl>
